spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: book_user
    password: book_password
    connection-timeout: 10s

management:
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans

